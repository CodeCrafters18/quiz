<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Quiz Platform</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            font-family: cursive;
            box-sizing: border-box;  
        }
        h1{
            position: relative;
            top: 300px;
            text-align: center;
            left: 25%;
            color: #ffffff;
            background-color:#007bffa7;
            width: 600px;
        }
        h3{
            color: white;
        }

        body {
            background-color: #f3f4f6;
            background-image: url("https://png.pngtree.com/background/20210710/original/pngtree-college-entrance-test-cartoon-education-banner-background-picture-image_992106.jpg");
            background-size: cover;
        }

        nav {
            background-color: #222222;
            padding: 20px 0;
            text-align: center;
            position: fixed;
            width: 100%;
            top: 0;
            z-index: 2;
        }

        nav img {
            width: 100px;
            height: auto;
        }

        nav ul {
            list-style: none;
            margin: 0;
            padding: 0;
        }

        nav ul li {
            display: inline-block;
            margin: 0 25px;
        }

        nav ul li a {
            text-decoration: none;
            color: #fff;
            font-family: cursive;
            font-weight: bold;
            font-size: 20px;
            transition: color 0.3s ease;
            margin: 20px;
        }

        nav ul li a:hover {
            color: #ffcc00;
        }

        .container {
            max-width: 1200px;
            margin: 100px auto 0;
            padding: 0 ;
            position: absolute;
            text-align: center;
            align-items: center;
            right:40%;
            top: 28%;
            width: 300px;
        }
         p{
            position: absolute;
            margin-bottom: 50px;
            font-size: large;
            font-weight: bold;
        }

        .btn-red {
            padding: 15px 30px;
            background-color: #252525;
            color: #fff;
            font-weight: bold;
            text-transform: uppercase;
            border: none;
            border-radius: 5px;
            cursor: pointer;
            transition: background-color 0.3s ease;
        }

        .btn-red:hover {
            background-color: #c2185b;
        }

        @media screen and (max-width: 768px) {
            .heading {
                font-size: 28px;
            }

            .btn-red {
                font-size: 14px;
            }
        }
    </style>
</head>

<body>
    <nav>
        <span style="color: white;margin-left: -1100px;">GameDev+</span> 
        <ul>
            <li><a href="home">Home</a></li>
            <li><a href="about">About Us</a></li>
            <li><a href="#contact">Contact Us</a></li>
            <li><% if(typeof username !== 'undefined' && username){ %>
                <h3>Hi <%= username %></h3>
                <% } else{ %>
                    <a href="/login">Login</a>
                <%}%>
            </li>
        </ul>
    </nav>
    <h1></h1>
    
    <div class="container">
        <form <% if(typeof username !== 'undefined' && username){ %>
            action="/test"
        <% }else{ %>
            action="/login"
        <% } %>>
        <% if(typeof username !== 'undefined' && username){ %>
            <button class="btn-red">start quiz</button>
        <% }else{ %>
            <button class="btn-red">Login to start</button>
        <% } %>
        </form><br>
        <% if(typeof logger !== 'undefined' && logger=="teacher"){ %>
        <li><a href="addquestion">Add more question</a></li><br>
        <% } %>
        <p>Hey champ dive into a great world of sharp minds</p>
    </div>
</body>
</html>